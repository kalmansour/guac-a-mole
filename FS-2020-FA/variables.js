// console.log("Hello World"); //In JS we can print strings

// console.log(3); //and we can print numbers
// console.log(`3`);

// console.log(3 + 5); //treat them as numbers ----> 8
// console.log(`3 + 5`); // treat them as strings ----> 3 + 5

// let numberOfRepos = 500;
// let username = "TheMshary";

// console.log(`The facebook username is ` + username);
// console.log(`The number of reps is` + numberOfRepos);

// console.log(`The facebook username is ${username}`);
// console.log(`The number of reps is ${numberOfRepos}`);

// console.log(`The Github username is ` + username); //concatination
// console.log(`${username} has 30 repos ` + username); //concatination

// console.log(`The Github username is ${username}`); //interpelating
// console.log(`${username} has ${numberOfRepos} repos`); //interpelating

username = `Lailz`;
let numberOfRepos = 500;
console.log(`The Github username is ${username}`); //interpelating
console.log(`${username} has ${numberOfRepos + 5} repos`); //interpelating

//interpelating method is much more efficient. Use that.

// const username = "TheMshary" //if i don't want the variable to change use const instead "Sealing the box"
